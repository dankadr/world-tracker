# Get your Google OAuth Client ID from https://console.cloud.google.com/
# Create OAuth 2.0 Client ID (Web application type)
# Add http://localhost:8088 to authorized JavaScript origins
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# Vite requires the VITE_ prefix to expose env vars to the frontend at build time
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# Change this to a random secret in production
JWT_SECRET=change-me-in-production

# Sentry DSN for error tracking (get from Sentry dashboard)
VITE_SENTRY_DSN=
