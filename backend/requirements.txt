# Backend requirements.txt – used by Docker (uvicorn + asyncpg).
# Also includes psycopg for local dev parity when testing against Neon.
# Versions pinned to prevent surprise breaking changes from upstream packages.
fastapi==0.135.1
uvicorn[standard]==0.41.0
sqlalchemy[asyncio]==2.0.48
asyncpg==0.31.0
psycopg[binary]==3.3.3
python-jose[cryptography]==3.5.0
cryptography>=42.0.0
google-auth==2.48.0
requests==2.32.3
pydantic==2.12.5
slowapi==0.1.9
resend>=2.0.0
# sentry-sdk is optional — install separately if needed (not bundled to stay
# under Vercel's 249 MB Lambda limit). The backend handles ImportError gracefully.
