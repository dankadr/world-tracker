# Backend requirements.txt – used by Docker (uvicorn + asyncpg).
# Also includes psycopg for local dev parity when testing against Neon.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
psycopg[binary]>=3.1.0
python-jose[cryptography]>=3.3.0
google-auth>=2.37.0
requests>=2.32.0
pydantic>=2.10.0
