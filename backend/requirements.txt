# Backend requirements.txt – used by Docker (uvicorn + asyncpg).
# Also includes psycopg for local dev parity when testing against Neon.
# Versions pinned to prevent surprise breaking changes from upstream packages.
fastapi==0.135.1
uvicorn[standard]==0.41.0
sqlalchemy[asyncio]==2.0.48
asyncpg==0.31.0
psycopg[binary]==3.3.3
python-jose[cryptography]==3.5.0
google-auth==2.48.0
requests==2.32.3
pydantic==2.12.5
# Optional: error tracking. Set SENTRY_DSN env var to enable.
sentry-sdk[fastapi]==2.27.0
